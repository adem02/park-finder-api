pipeline {
    agent any
    stages {
        stage('Info') {
            steps {
                echo 'Jenkinsfile lu depuis le repo Git !'
                sh 'pwd'
                sh 'ls -la'
            }
        }
    }
}
