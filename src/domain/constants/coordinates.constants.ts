export const FRANCE_BOUNDS = {
  minLat: 41.0,
  maxLat: 51.5,
  minLng: -5.5,
  maxLng: 10.0,
};

export const VALID_COORDINATES = {
  minLat: -90,
  maxLat: 90,
  minLng: -180,
  maxLng: 180,
};

export const EARTH_RADIUS_METERS = 6_371_000;
export const TO_RADIANS = Math.PI / 180;

export const DUPLICATE_PARKING_RADIUS_METERS = 50;
